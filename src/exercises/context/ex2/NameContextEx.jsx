import React from 'react'
import { createContext } from 'react'

// Create the contect (build the radio station).
// that's it just 1 line we created radio station called NameContext.
export const NameContext = createContext();
